@model IEnumerable<BolsaTrabajoV1.Models.HABILIDAD>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.NOMBREHABILIDAD)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CURRICULUM.IDCURRICULUM)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TIPO_HABILIDAD.NOMBRETIPOHABILIDAD)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.NOMBREHABILIDAD)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CURRICULUM.IDCURRICULUM)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TIPO_HABILIDAD.NOMBRETIPOHABILIDAD)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IDHABILIDAD }) |
            @Html.ActionLink("Details", "Details", new { id=item.IDHABILIDAD }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IDHABILIDAD })
        </td>
    </tr>
}

</table>
