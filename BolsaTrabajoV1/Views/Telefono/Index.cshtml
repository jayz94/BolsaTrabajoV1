@model IEnumerable<BolsaTrabajoV1.Models.TELEFONO>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.NUMEROTELEFONO)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.POSTULANTE.NOMBREPOSTULANTE)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TIPO_TELEFONO.NOMBRETIPOTELEFONO)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.NUMEROTELEFONO)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.POSTULANTE.NOMBREPOSTULANTE)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TIPO_TELEFONO.NOMBRETIPOTELEFONO)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IDTELEFONO }) |
            @Html.ActionLink("Details", "Details", new { id=item.IDTELEFONO }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IDTELEFONO })
        </td>
    </tr>
}

</table>
