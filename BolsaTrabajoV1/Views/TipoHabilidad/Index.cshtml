@model IEnumerable<BolsaTrabajoV1.Models.TIPO_HABILIDAD>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table" style="color:black;" data-toggle="table" datasrc="true" data-search="true"
 data-select-item-name="toolbar1" data-pagination="true" data-sort-name="NOMBREAREACONOCIMIENTO"
 data-sort-order="asc">
    <thead>
        <tr>
            <th data-field="NOMBRETIPOHABILIDAD" data-sortable="true">
                @Html.DisplayNameFor(model => model.NOMBRETIPOHABILIDAD)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.AREA_CONOCIMIENTO.NOMBREAREACONOCIMIENTO)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.NOMBRETIPOHABILIDAD)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.AREA_CONOCIMIENTO.NOMBREAREACONOCIMIENTO)
                </td>
                <td>
                    @Html.ActionLink("Edit", "Edit", new { id = item.IDTIPOHABILIDAD }) |
                    @Html.ActionLink("Details", "Details", new { id = item.IDTIPOHABILIDAD }) |
                    @Html.ActionLink("Delete", "Delete", new { id = item.IDTIPOHABILIDAD })
                </td>
            </tr>
        }
    </tbody>
</table>
