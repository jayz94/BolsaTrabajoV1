@model IEnumerable<BolsaTrabajoV1.Models.TIPO_CONTACTO>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.NOMBRETIPOCONTACTO)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.NOMBRETIPOCONTACTO)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IDTIPOCONTACTO }) |
            @Html.ActionLink("Details", "Details", new { id=item.IDTIPOCONTACTO }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IDTIPOCONTACTO })
        </td>
    </tr>
}

</table>
