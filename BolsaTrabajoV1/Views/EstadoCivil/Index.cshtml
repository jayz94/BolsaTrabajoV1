@model IEnumerable<BolsaTrabajoV1.Models.ESTADO_CIVIL>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table" style="color:black;" data-toggle="table" datasrc="true" data-search="true"
       data-select-item-name="toolbar1" data-pagination="true" data-sort-name="NOMBREAREACONOCIMIENTO"
       data-sort-order="asc">
    <thead>
        <tr>
            <th data-field="NOMBREESTADOCIVIL" data-sortable="true">
                @Html.DisplayNameFor(model => model.NOMBREESTADOCIVIL)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.NOMBREESTADOCIVIL)
                </td>
                <td>
                    @Html.ActionLink("Edit", "Edit", new { id = item.IDESTADOCIVIL }) |
                    @Html.ActionLink("Details", "Details", new { id = item.IDESTADOCIVIL }) |
                    @Html.ActionLink("Delete", "Delete", new { id = item.IDESTADOCIVIL })
                </td>
            </tr>
        }
    </tbody>
</table>
