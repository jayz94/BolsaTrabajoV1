@model IEnumerable<BolsaTrabajoV1.Models.TIPO_EXAMEN>

@{
    ViewBag.Title = "Index";
}

<h2 class="text-center">Tipos Examen</h2>

<p>
    @Html.ActionLink("Agregar", "Create", null, new { @class = "btn btn-success" })
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DESCRIPCIONTIPO)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DESCRIPCIONTIPO)
        </td>
        <td>
            @Html.ActionLink(" ", "Edit", new { id=item.IDTIPOEXAMEN }, new { @class = "btn btn-success fa fa-edit btn-xs" }) 
            @Html.ActionLink(" ", "Details", new { id=item.IDTIPOEXAMEN }, new { @class = "btn btn-primary fa fa-search btn-xs" }) 
            @Html.ActionLink(" ", "Delete", new { id=item.IDTIPOEXAMEN }, new { @class = "btn btn-danger fa fa-trash btn-xs" })
        </td>
    </tr>
}

</table>
