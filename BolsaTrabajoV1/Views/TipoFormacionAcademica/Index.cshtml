@model IEnumerable<BolsaTrabajoV1.Models.TIPOFORMACINONACADEMICA>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table" style="color:black;" data-toggle="table" datasrc="true" data-search="true"
 data-select-item-name="toolbar1" data-pagination="true" data-sort-name="NOMBREAREACONOCIMIENTO"
 data-sort-order="asc">
    <thead>
        <tr>
            <th data-field="NOMBRETIPOFORMACION" data-sortable="true">
                @Html.DisplayNameFor(model => model.NOMBRETIPOFORMACION)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.NOMBRETIPOFORMACION)
                </td>
                <td>
                    @Html.ActionLink("Edit", "Edit", new { id = item.IDTIPOFORMACION }) |
                    @Html.ActionLink("Details", "Details", new { id = item.IDTIPOFORMACION }) |
                    @Html.ActionLink("Delete", "Delete", new { id = item.IDTIPOFORMACION })
                </td>
            </tr>
        }
    </tbody>
</table>

